<% if @moxibustion.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@moxibustion.errors.count, "error") %>
    </div>
    <ul>
    <% @moxibustion.errors.full_messages.each do |msg| %>
      <li>* <%= msg %> </li>
    <% end %>
    </ul>
  </div>
<% end %>
   
