<% if @cupping.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@cupping.errors.count, "error") %>
    </div>
    <ul>
    <% @cupping.errors.full_messages.each do |msg| %>
      <li>* <%= msg %> </li>
    <% end %>
    </ul>
  </div>
<% end %>
   
